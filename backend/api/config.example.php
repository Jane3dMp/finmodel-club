<?php
// Финмодель «Прознание + CODDY» — конфигурация
//
// ⚠️ ЭТОТ ФАЙЛ СОДЕРЖИТ ПАРОЛИ. Не добавлять в git, не выкладывать публично.
// Заполнить данными, которые дал хостинг-провайдер.

return [
    // Параметры MySQL (взять в панели хостинга)
    'db' => [
        'host'     => 'localhost',        // обычно localhost
        'name'     => 'ЗАМЕНИТЬ_имя_бд',
        'user'     => 'ЗАМЕНИТЬ_логин',
        'password' => 'ЗАМЕНИТЬ_пароль',
        'charset'  => 'utf8mb4',
    ],

    // Простой токен доступа. Должен совпадать с токеном в index.html.
    // Сгенерировать случайную строку, например: openssl rand -hex 24
    'auth_token' => 'ЗАМЕНИТЬ_на_случайную_строку',

    // Разрешённые источники (CORS).
    // Если клиент лежит на том же домене — можно оставить пустым.
    // Если временно на GitHub Pages — добавить его адрес.
    'allowed_origins' => [
        'https://jane3dmp.github.io',
    ],
];
