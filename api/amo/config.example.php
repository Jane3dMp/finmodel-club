<?php
// Прокси к amoCRM — конфигурация.
//
// ⚠️ ЭТОТ ФАЙЛ СОДЕРЖИТ СЕКРЕТНЫЙ ТОКЕН amoCRM.
//    • НЕ добавлять в git (config.php уже в .gitignore).
//    • Создать копию как config.php ПРЯМО НА СЕРВЕРЕ (файловый менеджер хостинга),
//      заполнить и сохранить. Автозаливка его не тронет (**/config.php в exclude).
//
// Где взять токен (делается один раз, ~3 минуты):
//   amoCRM → Настройки → Интеграции → «+ Создать интеграцию» → «Внешняя интеграция»
//   → название любое («Финмодель»), доступ: чтение сделок и контактов
//   → после сохранения вкладка «Ключи и доступы» → «Ключ долгосрочного доступа» → скопировать.
// Токен длинный (несколько сотен символов) — вставить целиком, одной строкой.

return [
    // Поддомен amoCRM: можно «proznanie» или полностью «proznanie.amocrm.ru».
    'subdomain'    => 'ЗАМЕНИТЬ_поддомен',

    // Долгосрочный токен доступа (Bearer).
    'access_token' => 'ЗАМЕНИТЬ_долгосрочный_токен',
];

// Кто имеет право дёргать прокси (firebase_project / allowed_emails / allowed_origins)
// берётся из api/alfa/config.php — авторизация у обоих прокси общая, чтобы не разъезжалась.
